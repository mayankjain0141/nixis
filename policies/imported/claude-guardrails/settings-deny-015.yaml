# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-015
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit ~/.claude/settings.json
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.claude/settings.json'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.claude/settings\\.json$")
          message: 'Blocked by imported settings.json rule: Edit ~/.claude/settings.json'
          action: DENY
    defaultAction: ALLOW
