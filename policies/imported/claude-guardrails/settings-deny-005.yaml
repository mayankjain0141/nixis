# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-005
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Read ~/.azure/**
spec:
    description: 'Deny rule imported from settings.json: Read ~/.azure/**'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*/\\.azure/.*")
          message: 'Blocked by imported settings.json rule: Read ~/.azure/**'
          action: DENY
    defaultAction: ALLOW
