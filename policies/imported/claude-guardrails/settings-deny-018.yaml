# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-018
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Bash(sudo *)
spec:
    description: 'Deny rule imported from settings.json: Bash(sudo *)'
    matchConstraints:
        tools: [Bash]
    validations:
        - expression: tool == "Bash" && request.args.command.matches("sudo .*")
          message: 'Blocked by imported settings.json rule: Bash(sudo *)'
          action: DENY
    defaultAction: ALLOW
