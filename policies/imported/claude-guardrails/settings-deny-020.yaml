# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-020
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Bash(dd *)
spec:
    description: 'Deny rule imported from settings.json: Bash(dd *)'
    matchConstraints:
        tools: [Bash]
    validations:
        - expression: tool == "Bash" && request.args.command.matches("dd .*")
          message: 'Blocked by imported settings.json rule: Bash(dd *)'
          action: DENY
    defaultAction: ALLOW
