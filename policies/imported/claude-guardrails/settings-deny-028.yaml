# imported from: claude-guardrails via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-028
    annotations:
        aegis.io/imported-from: aegis-import-3430838287.json
        aegis.io/severity: medium
        aegis.io/source-rule: Edit ~/.bashrc
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.bashrc'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.bashrc$")
          message: 'Blocked by imported settings.json rule: Edit ~/.bashrc'
          action: DENY
    defaultAction: ALLOW
