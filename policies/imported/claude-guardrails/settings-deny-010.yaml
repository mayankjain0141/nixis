# imported from: claude-guardrails via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-010
    annotations:
        aegis.io/imported-from: aegis-import-3634843882.json
        aegis.io/severity: medium
        aegis.io/source-rule: Read **/*.pem
spec:
    description: 'Deny rule imported from settings.json: Read **/*.pem'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*\\.pem$")
          message: 'Blocked by imported settings.json rule: Read **/*.pem'
          action: DENY
    defaultAction: ALLOW
