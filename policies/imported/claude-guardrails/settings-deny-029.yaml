# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-029
    annotations:
        nixis.io/imported-from: nixis-import-3430838287.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit ~/.zshrc
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.zshrc'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.zshrc$")
          message: 'Blocked by imported settings.json rule: Edit ~/.zshrc'
          action: DENY
    defaultAction: ALLOW
