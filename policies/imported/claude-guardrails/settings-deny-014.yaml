# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-014
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Read ~/.git-credentials
spec:
    description: 'Deny rule imported from settings.json: Read ~/.git-credentials'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*/\\.git-credentials$")
          message: 'Blocked by imported settings.json rule: Read ~/.git-credentials'
          action: DENY
    defaultAction: ALLOW
