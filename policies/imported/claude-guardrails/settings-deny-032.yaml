# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-032
    annotations:
        nixis.io/imported-from: nixis-import-3430838287.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit ~/.profile
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.profile'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.profile$")
          message: 'Blocked by imported settings.json rule: Edit ~/.profile'
          action: DENY
    defaultAction: ALLOW
