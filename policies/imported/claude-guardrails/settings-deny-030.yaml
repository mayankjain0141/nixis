# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-030
    annotations:
        nixis.io/imported-from: nixis-import-3430838287.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit ~/.bash_profile
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.bash_profile'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.bash_profile$")
          message: 'Blocked by imported settings.json rule: Edit ~/.bash_profile'
          action: DENY
    defaultAction: ALLOW
