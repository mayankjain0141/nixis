# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-012
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Read ~/.npmrc
spec:
    description: 'Deny rule imported from settings.json: Read ~/.npmrc'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*/\\.npmrc$")
          message: 'Blocked by imported settings.json rule: Read ~/.npmrc'
          action: DENY
    defaultAction: ALLOW
