# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-031
    annotations:
        nixis.io/imported-from: nixis-import-3430838287.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit ~/.zprofile
spec:
    description: 'Deny rule imported from settings.json: Edit ~/.zprofile'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*/\\.zprofile$")
          message: 'Blocked by imported settings.json rule: Edit ~/.zprofile'
          action: DENY
    defaultAction: ALLOW
