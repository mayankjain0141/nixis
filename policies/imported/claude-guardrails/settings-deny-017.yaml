# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-017
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Edit **/.env.*
spec:
    description: 'Deny rule imported from settings.json: Edit **/.env.*'
    matchConstraints:
        tools: [Edit, Write]
    validations:
        - expression: (tool == "Edit" || tool == "Write") && request.args.path.matches(".*\\.env\\..*")
          message: 'Blocked by imported settings.json rule: Edit **/.env.*'
          action: DENY
    defaultAction: ALLOW
