# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-027
    annotations:
        nixis.io/imported-from: nixis-import-3430838287.json
        nixis.io/severity: medium
        nixis.io/source-rule: Read ~/.electrum/**
spec:
    description: 'Deny rule imported from settings.json: Read ~/.electrum/**'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*/\\.electrum/.*")
          message: 'Blocked by imported settings.json rule: Read ~/.electrum/**'
          action: DENY
    defaultAction: ALLOW
