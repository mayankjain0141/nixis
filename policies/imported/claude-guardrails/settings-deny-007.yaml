# imported from: claude-guardrails via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: settings-deny-007
    annotations:
        nixis.io/imported-from: nixis-import-3634843882.json
        nixis.io/severity: medium
        nixis.io/source-rule: Read ~/.docker/config.json
spec:
    description: 'Deny rule imported from settings.json: Read ~/.docker/config.json'
    matchConstraints:
        tools: [Read]
    validations:
        - expression: tool == "Read" && request.args.path.matches(".*/\\.docker/config\\.json")
          message: 'Blocked by imported settings.json rule: Read ~/.docker/config.json'
          action: DENY
    defaultAction: ALLOW
