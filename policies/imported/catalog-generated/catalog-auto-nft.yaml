# auto-generated from pkg/adapters/catalog.json via nixis policy generate-from-catalog
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-nft
    annotations:
        nixis.io/family: bash
        nixis.io/risk-level: critical
        nixis.io/severity: critical
        nixis.io/source: pkg/adapters/catalog.json
spec:
    description: Deny critically-risky nft (bash)
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.startsWith("nft")
          message: nft requires approval — confirm this is the intended operation
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
