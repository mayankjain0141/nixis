# auto-generated from pkg/adapters/catalog.json via aegis policy generate-from-catalog
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-masscan
    annotations:
        aegis.io/family: bash
        aegis.io/risk-level: critical
        aegis.io/severity: critical
        aegis.io/source: pkg/adapters/catalog.json
spec:
    description: Deny critically-risky masscan (bash)
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.startsWith("masscan")
          message: masscan is blocked — this operation is not appropriate for an AI agent
          action: DENY
    defaultAction: ALLOW
