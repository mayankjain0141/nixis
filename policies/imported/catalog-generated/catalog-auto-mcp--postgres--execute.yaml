# auto-generated from pkg/adapters/catalog.json via nixis policy generate-from-catalog
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-mcp--postgres--execute
    annotations:
        nixis.io/family: mcp
        nixis.io/risk-level: high
        nixis.io/severity: high
        nixis.io/source: pkg/adapters/catalog.json
spec:
    description: 'Require approval for mcp__postgres__execute (mcp risk level: high)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "mcp__postgres__execute"
          message: mcp__postgres__execute is classified as high risk — approval required
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
