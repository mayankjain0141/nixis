# auto-generated from pkg/adapters/catalog.json via nixis policy generate-from-catalog
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-parted
    annotations:
        nixis.io/family: bash
        nixis.io/risk-level: critical
        nixis.io/severity: critical
        nixis.io/source: pkg/adapters/catalog.json
spec:
    description: Deny critically-risky parted (bash)
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.startsWith("parted")
          message: parted is blocked — this operation is not appropriate for an AI agent
          action: DENY
    defaultAction: ALLOW
