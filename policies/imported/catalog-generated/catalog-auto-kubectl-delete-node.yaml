# auto-generated from pkg/adapters/catalog.json via aegis policy generate-from-catalog
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-kubectl-delete-node
    annotations:
        aegis.io/family: bash
        aegis.io/risk-level: critical
        aegis.io/severity: critical
        aegis.io/source: pkg/adapters/catalog.json
spec:
    description: Deny critically-risky kubectl delete node (bash)
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.startsWith("kubectl delete node")
          message: kubectl delete node requires approval — confirm this is the intended operation
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
