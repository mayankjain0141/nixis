# auto-generated from pkg/adapters/catalog.json via aegis policy generate-from-catalog
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: catalog-auto-git-reset---hard
    annotations:
        aegis.io/family: bash
        aegis.io/risk-level: high
        aegis.io/severity: high
        aegis.io/source: pkg/adapters/catalog.json
spec:
    description: 'Require approval for git reset --hard (bash risk level: high)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.startsWith("git reset --hard")
          message: git reset --hard is classified as high risk — approval required
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
