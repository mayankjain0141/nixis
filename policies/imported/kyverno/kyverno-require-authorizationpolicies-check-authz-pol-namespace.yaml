# IMPORT_TODO: JMESPath conditions in require-authorizationpolicies/check-authz-pol could not be translated — cross-resource check (namespace must have AuthorizationPolicy) is not expressible in single-request CEL; translated to AUDIT
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-require-authorizationpolicies-check-authz-pol-namespace
    annotations:
        aegis.io/imported-from: aegis-import-2673448083.yaml
        aegis.io/severity: medium
        kyverno.io/category: Istio
spec:
    description: An AuthorizationPolicy is used to provide access controls for traffic in the mesh and can be defined at multiple levels. For the Namespace level, all Namespaces should have at least one AuthorizationPolicy. This policy, designed to run in background mode for reporting purposes, ensures every Namespace has at least one AuthorizationPolicy.
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: 'false'
          message: 'Cross-resource policy translated to audit-only — Aegis cannot verify that a Namespace has an AuthorizationPolicy within a single request context'
          action: AUDIT
    defaultAction: ALLOW
