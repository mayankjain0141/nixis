# IMPORT_REVIEW: translated by Claude — verify semantics
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-copy-namespace-labels-all-label-deployment-from-ns
    annotations:
        aegis.io/imported-from: aegis-import-184284637.yaml
        aegis.io/severity: medium
        aegis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: It is common for Namespaced resources to need access to labels which have been assigned to the Namespace in which they reside. This policy demonstrates two different ways of assigning Namespace labels to a Deployment. The first method copies only the `owner` label while the second copies all labels except for `kubernetes.io/metadata.name`.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'IMPORT_REVIEW: mutate rule — Aegis does not mutate requests — manual review required'
          action: AUDIT
    defaultAction: ALLOW
