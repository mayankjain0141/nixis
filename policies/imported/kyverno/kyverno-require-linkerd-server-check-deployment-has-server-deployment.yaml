# IMPORT_TODO: JMESPath conditions in require-linkerd-server/check-deployment-has-server could not be translated — kubectl pattern match generated as base condition only
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-require-linkerd-server-check-deployment-has-server-deployment
    annotations:
        aegis.io/imported-from: aegis-import-1261990201.yaml
        aegis.io/severity: medium
        kyverno.io/category: Linkerd
spec:
    description: In Linkerd 2.11, a Server resource selects ports on a set of Pods in the same Namespace and is used to deny traffic which then must be authorized later. Ensuring that Linkerd policy is enforced on Pods in the mesh is important to maintaining a secure environment. This policy, requiring Linkerd 2.11+, has two rules designed to check Deployments (exposing ports) and Services to ensure a corresponding Server resource exists first.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("(?i)kubectl.*(delete\s+deployment|scale|rollout)")
          message: Every Deployment declaring ports requires a matching Server.
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
