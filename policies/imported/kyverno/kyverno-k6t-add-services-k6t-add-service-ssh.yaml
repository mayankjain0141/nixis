# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-k6t-add-services-k6t-add-service-ssh
    annotations:
        nixis.io/imported-from: nixis-import-3081765384.yaml
        nixis.io/severity: low
        kyverno.io/category: KubeVirt
spec:
    description: Add an SSH Service to every VirtualMachineInstance which is getting created. This Service will use a ClusterIP, thus the admin has to ensure that the IP space is large enough and ClusterIP type can be met.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Generate rule — Nixis does not generate resources — manual review required'
          action: AUDIT
    defaultAction: ALLOW
