# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-verify-flux-images-verify-cosign-signature-pod
    annotations:
        nixis.io/imported-from: nixis-import-3526291889.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: low
        kyverno.io/category: Flux
spec:
    description: Ensures that container images used to run Flux controllers in the cluster are signed with valid Cosign signatures. Prevents the deployment of untrusted or potentially compromised Flux images. Protects the integrity and security of the Flux deployment process.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Flux controller image cosign signature verification requires cryptographic registry API call — cannot verify image signatures from manifest content alone; verify Flux controller images are signed via CI/CD pipeline'
          action: AUDIT
    defaultAction: ALLOW
