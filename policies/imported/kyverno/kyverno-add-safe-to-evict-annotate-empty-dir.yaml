# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-safe-to-evict-annotate-empty-dir
    annotations:
        aegis.io/imported-from: aegis-import-2208999353.yaml
        aegis.io/severity: low
        aegis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: 'The Kubernetes cluster autoscaler does not evict pods that  use hostPath or emptyDir volumes. To allow eviction of these pods, the annotation  cluster-autoscaler.kubernetes.io/safe-to-evict=true must be added to the pods. '
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (safe-to-evict annotation for emptyDir) — Aegis cannot mutate requests; verify pods with emptyDir volumes set cluster-autoscaler.kubernetes.io/safe-to-evict=true annotation'
          action: AUDIT
    defaultAction: ALLOW
