# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-safe-to-evict-annotate-empty-dir
    annotations:
        nixis.io/imported-from: nixis-import-2208999353.yaml
        nixis.io/severity: low
        nixis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: 'The Kubernetes cluster autoscaler does not evict pods that  use hostPath or emptyDir volumes. To allow eviction of these pods, the annotation  cluster-autoscaler.kubernetes.io/safe-to-evict=true must be added to the pods. '
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (safe-to-evict annotation for emptyDir) — Nixis cannot mutate requests; verify pods with emptyDir volumes set cluster-autoscaler.kubernetes.io/safe-to-evict=true annotation'
          action: AUDIT
    defaultAction: ALLOW
