# IMPORT_REVIEW: Cross-resource numeric equality check — Aegis checks for PodDisruptionBudget manifests with minAvailable of 0 (always blocks disruptions); full replica-count comparison requires Kyverno
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-pdb-minavailable-check-pdb-minavailable-deployment
    annotations:
        aegis.io/imported-from: aegis-import-2709280891.yaml
        aegis.io/severity: low
        aegis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: When a Pod controller which can run multiple replicas is subject to an active PodDisruptionBudget, if the replicas field has a value equal to the minAvailable value of the PodDisruptionBudget it may prevent voluntary disruptions including Node drains which may impact routine maintenance tasks and disrupt operations. This policy checks incoming Deployments and StatefulSets which have a matching PodDisruptionBudget to ensure these two values do not match.
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: "(tool == \"Write\" || tool == \"Edit\") && request.args.path.matches(\".*\\\\.(yaml|yml|json)$\") && request.args.content.contains(\"kind: PodDisruptionBudget\") && (request.args.content.contains(\"minAvailable: 0\") || request.args.content.contains(\"minAvailable: '0'\") || request.args.content.contains('minAvailable: \"0\"'))"
          message: 'PodDisruptionBudget minAvailable is set to 0 which blocks all voluntary disruptions including node drains — set minAvailable to at least 1'
          action: DENY
    defaultAction: ALLOW
