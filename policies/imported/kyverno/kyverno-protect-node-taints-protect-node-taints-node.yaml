# IMPORT_REVIEW: translated by Claude — kubectl taint node intercepted at Bash level; Write/Edit pathway DENYs Node manifests that include a taints section (applying such manifests would alter node taints); cluster-admin bypass check requires RBAC context not available in Aegis CEL
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-protect-node-taints-protect-node-taints-node
    annotations:
        aegis.io/imported-from: aegis-import-3280031117.yaml
        aegis.io/severity: low
        aegis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: 'Node taints are often used as a control in multi-tenant use cases. If users can alter them, they may be able to affect scheduling of Pods which may impact other workloads. This sample prohibits altering of node taints unless by a user holding the `cluster-admin` ClusterRole. Use of this policy requires removal of the Node resource filter in the Kyverno ConfigMap ([Node,*,*]). Due to Kubernetes CVE-2021-25735, this policy requires, at minimum, one of the following versions of Kubernetes: v1.18.18, v1.19.10, v1.20.6, or v1.21.0.'
    matchConstraints:
        tools:
            - Bash
            - Write
            - Edit
    validations:
        - expression: 'tool == "Bash" && request.args.command.matches(".*kubectl.*taint.*node.*")'
          message: 'Altering node taints is not allowed — only cluster-admin ClusterRole holders may modify node taints'
          action: DENY
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.matches(".*\\.(yaml|yml|json)$") && request.args.content.contains("kind: Node") && request.args.content.contains("taints:")'
          message: 'Node manifest includes a taints section — applying this manifest would alter node taints; only cluster-admin ClusterRole holders may modify node taints'
          action: DENY
    defaultAction: ALLOW
