# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-pdb-maxunavailable-pdb-maxunavailable-poddisruptionbudget
    annotations:
        aegis.io/imported-from: aegis-import-1235231053.yaml
        aegis.io/severity: low
        kyverno.io/category: Other
spec:
    description: A PodDisruptionBudget which sets its maxUnavailable value to zero prevents all voluntary evictions including Node drains which may impact maintenance tasks. This policy enforces that if a PodDisruptionBudget specifies the maxUnavailable field it must be greater than zero.
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.matches(".*\\.(yaml|yml|json)$") && request.args.content.contains("PodDisruptionBudget") && request.args.content.contains("maxUnavailable: 0")'
          message: 'PodDisruptionBudget maxUnavailable must be greater than zero'
          action: DENY
    defaultAction: ALLOW
