# IMPORT_TODO: generate rule — Aegis does not generate resources
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-kasten-generate-policy-by-preset-label-kasten-generate-policy-by-preset-label
    annotations:
        aegis.io/imported-from: aegis-import-3807547480.yaml
        aegis.io/severity: low
        kyverno.io/category: Veeam Kasten
spec:
    description: Generates a Kasten policy for a new namespace that includes a valid "dataprotection" label, if the policy does not already exist. Use with "kasten-validate-ns-by-preset-label" policy to require "dataprotection" labeling on new namespaces.
    matchConstraints:
        tools: []
    validations:
        - expression: "false"
          message: 'IMPORT_TODO: generate rule — Aegis does not generate resources — manual review required'
          action: AUDIT
    defaultAction: ALLOW
