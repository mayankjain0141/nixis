# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-ndots-add-ndots
    annotations:
        nixis.io/imported-from: nixis-import-4038978166.yaml
        nixis.io/severity: low
        nixis.io/llm-confidence: medium
        kyverno.io/category: Sample
spec:
    description: The ndots value controls where DNS lookups are first performed in a cluster and needs to be set to a lower value than the default of 5 in some cases. This policy mutates all Pods to add the ndots option with a value of 1.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (DNS ndots option) — Nixis cannot mutate requests; verify pods set dnsConfig.options ndots to 1 if DNS lookup latency optimization is required'
          action: AUDIT
    defaultAction: ALLOW
