# IMPORT_REVIEW: translated by Claude — verify semantics
# imported from: policies via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-schedule-based-quotas-update-quotas-by-schedule
    annotations:
        aegis.io/imported-from: aegis-import-1357795143.yaml
        aegis.io/severity: medium
        aegis.io/llm-confidence: medium
        kyverno.io/category: Resource Management
spec:
    description: Automatically adjusts resource quotas based on time schedules to optimize cloud costs. During non-business hours, the policy reduces resource quotas to prevent overprovisioning while ensuring essential services remain operational.
    matchConstraints:
        tools: []
    validations:
        - expression: 'tool == "Bash" && request.args.command.matches(".*kubectl\\s+(apply|create|patch|run).*")'
          message: 'IMPORT_REVIEW: mutate rule — Aegis does not mutate requests — manual review required'
          action: AUDIT
    defaultAction: ALLOW
