# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-env-vars-from-cm-add-env-vars-from-cm
    annotations:
        nixis.io/imported-from: nixis-import-3873651939.yaml
        nixis.io/severity: low
        nixis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: 'Instead of defining a common set of environment variables multiple times either in manifests or separate policies, Pods can reference entire collections stored in a ConfigMap. This policy mutates all initContainers (if present) and containers in a Pod with environment variables defined in a ConfigMap named `nsenvvars` that must exist in the destination Namespace.     '
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (env vars from ConfigMap) — Nixis cannot mutate requests; verify pods reference environment variables from the nsenvvars ConfigMap as required'
          action: AUDIT
    defaultAction: ALLOW
