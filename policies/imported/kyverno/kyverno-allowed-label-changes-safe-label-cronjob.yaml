# imported from: policies via aegis policy import
# IMPORT_REVIEW: cross-resource check — label-change detection requires old+new diff state unavailable at request-time; best-effort audit only
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-allowed-label-changes-safe-label-cronjob
    annotations:
        aegis.io/imported-from: aegis-import-1584203326.yaml
        aegis.io/severity: medium
        kyverno.io/category: Other
spec:
    description: In some cases, operations teams need a type of limited access to change resources during troubleshooting or outage mitigation. This policy demonstrates how to prevent modification to labels except one with the key `breakglass`. Changing, adding, or deleting any other labels is denied.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Cannot be enforced at request-time: label-change detection (adding/removing/modifying non-breakglass labels) requires old-state diff which is unavailable in single-request CEL evaluation.'
          action: AUDIT
    defaultAction: ALLOW
