# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-copy-namespace-labels-owner-label-deployment-from-ns
    annotations:
        nixis.io/imported-from: nixis-import-184284637.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: It is common for Namespaced resources to need access to labels which have been assigned to the Namespace in which they reside. This policy demonstrates two different ways of assigning Namespace labels to a Deployment. The first method copies only the `owner` label while the second copies all labels except for `kubernetes.io/metadata.name`.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (copy owner namespace label to Deployment) — Nixis cannot mutate requests; verify Deployments that need the namespace owner label set it explicitly in metadata.labels'
          action: AUDIT
    defaultAction: ALLOW
