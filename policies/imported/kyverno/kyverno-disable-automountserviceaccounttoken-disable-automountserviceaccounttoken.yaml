# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-disable-automountserviceaccounttoken-disable-automountserviceaccounttoken
    annotations:
        nixis.io/imported-from: nixis-import-2479192399.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: medium
        kyverno.io/category: Other, EKS Best Practices
spec:
    description: A new ServiceAccount called `default` is created whenever a new Namespace is created. Pods spawned in that Namespace, unless otherwise set, will be assigned this ServiceAccount. This policy mutates any new `default` ServiceAccounts to disable auto-mounting of the token into Pods obviating the need to do so individually.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (disable automountServiceAccountToken) — Nixis cannot mutate requests; verify the default ServiceAccount in each namespace sets automountServiceAccountToken=false'
          action: AUDIT
    defaultAction: ALLOW
