# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-node-affinity-add-node-affinity-deployment
    annotations:
        nixis.io/imported-from: nixis-import-719043267.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: medium
        kyverno.io/category: Other
spec:
    description: Node affinity, similar to node selection, is a way to specify which node(s) on which Pods will be scheduled but based on more complex conditions. This policy will add node affinity to a Deployment and if one already exists an expression will be added to it.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (node affinity injection) — Nixis cannot mutate requests; verify Deployments with node-affinity requirements define affinity expressions manually'
          action: AUDIT
    defaultAction: ALLOW
