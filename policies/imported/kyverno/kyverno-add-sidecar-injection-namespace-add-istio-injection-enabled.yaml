# imported from: policies via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: kyverno-add-sidecar-injection-namespace-add-istio-injection-enabled
    annotations:
        nixis.io/imported-from: nixis-import-3933823744.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: medium
        kyverno.io/category: Istio
spec:
    description: In order for Istio to inject sidecars to workloads deployed into Namespaces, the label `istio-injection` must be set to `enabled`. As an alternative to rejecting Namespace definitions which don't already contain this label, it can be added automatically. This policy adds the label `istio-inject` set to `enabled` for all new Namespaces.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Mutate rule (Istio sidecar injection label) — Nixis cannot mutate requests; verify namespaces requiring Istio sidecar injection have the istio-injection=enabled label'
          action: AUDIT
    defaultAction: ALLOW
