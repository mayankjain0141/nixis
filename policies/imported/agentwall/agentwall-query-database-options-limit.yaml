# imported from: agentwall via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: agentwall-query-database-options-limit
    annotations:
        aegis.io/imported-from: aegis-import-1966544447.yaml
        aegis.io/severity: low
spec:
    description: 'AgentWall constraint: query_database.options.limit'
    matchConstraints:
        tools:
            - query_database
    validations:
        - expression: tool == "query_database" && request.args.limit > 100
          message: AgentWall constraint violation on options.limit
          action: DENY
    defaultAction: ALLOW
