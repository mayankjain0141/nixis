# IMPORT_REVIEW: LLM-translated from agentwall — verify semantics (attempts: 1)
# imported from: agentwall via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: agentwall-query-database-options-filters
    annotations:
        nixis.io/imported-from: nixis-import-3739323068.yaml
        nixis.io/severity: high
        nixis.io/llm-confidence: medium
spec:
    description: 'AgentWall constraint: query_database.options.filters'
    matchConstraints:
        tools:
            - query_database
    validations:
        - expression: tool == "query_database" && "filters" in request.args
          message: 'IMPORT_REVIEW: original constraint was object schema validation — verify intent (deny filters entirely vs validate filter structure)'
          action: DENY
    defaultAction: ALLOW
