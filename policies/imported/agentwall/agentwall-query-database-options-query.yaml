# imported from: agentwall via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: agentwall-query-database-options-query
    annotations:
        aegis.io/imported-from: aegis-import-3739323068.yaml
        aegis.io/severity: high
spec:
    description: 'AgentWall constraint: query_database.options.query'
    matchConstraints:
        tools:
            - query_database
    validations:
        - expression: 'tool == "query_database" && !request.args.query.matches("(?i)^\\s*SELECT\\b.*")'
          message: 'AgentWall: deny non-SELECT queries — read-only enforcement'
          action: DENY
    defaultAction: ALLOW
