# IMPORT_TODO: Falco condition uses kernel or macro fields that cannot be automatically translated — manual review required. Original condition: outbound and container and fd.sip="169.254.169.254" and not user_known_metadata_access
# imported from: rules via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: falco-contact-cloud-metadata-service-from-container
    annotations:
        aegis.io/falco-tags: maturity_incubating,network,container,mitre_discovery,T1565
        aegis.io/imported-from: aegis-import-2621062960.yaml
        aegis.io/severity: low
        aegis.io/source-rule: Contact cloud metadata service from container
spec:
    description: |
        Detects attempts to communicate with the Cloud Instance Metadata Service from a container. This detection is narrowly focused and might not apply to your environment. In addition, it could generate noise and require fine-tuning.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: "false"
          message: 'Falco rule violated: Contact cloud metadata service from container'
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
