# imported from: rules via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: falco-wrong-mitre-rule
    annotations:
        nixis.io/falco-tags: container,mitre_lateral_movement,T1610
        nixis.io/imported-from: nixis-import-1844418014.yaml
        nixis.io/severity: medium
        nixis.io/llm-confidence: medium
        nixis.io/source-rule: wrong mitre rule
spec:
    description: |
        Detect the initial process started by a container that is not in a list of allowed containers.
    matchConstraints:
        tools: []
    validations:
        - expression: 'false'
          message: 'Falco rule violated: wrong mitre rule'
          action: AUDIT
    defaultAction: ALLOW
