# imported from: rules via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: falco-correct-mitre-rule
    annotations:
        aegis.io/falco-tags: host,container,network,mitre_persistence,T1098
        aegis.io/imported-from: aegis-import-1844418014.yaml
        aegis.io/severity: high
        aegis.io/llm-confidence: medium
        aegis.io/source-rule: correct mitre rule
spec:
    description: an attempt to write to any file below /etc
    matchConstraints:
        tools:
            - Write
            - Edit
            - Bash
    validations:
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.startsWith("/etc/") || tool == "Bash" && request.args.command.matches(".*>\\s*/etc/.*")'
          message: 'Falco rule violated: correct mitre rule'
          action: DENY
    defaultAction: ALLOW
