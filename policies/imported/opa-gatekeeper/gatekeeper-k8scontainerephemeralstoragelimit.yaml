# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sContainerEphemeralStorageLimit
# REGO LOGIC: package k8scontainerephemeralstoragelimit  import data.lib.exclude_update.is_update import data.lib.exempt_container.is_exempt  missing(obj, field) = true {   not obj[field] }  missing(obj, field) = t...
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sContainerEphemeralStorageLimit' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8scontainerephemeralstoragelimit
    annotations:
        aegis.io/imported-from: aegis-import-2333734467.yaml
        aegis.io/original-kind: K8sContainerEphemeralStorageLimit
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: |-
        Requires containers to have an ephemeral storage limit set and constrains the limit to be within the specified maximum values.
        https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: |-
            Requires containers to have an ephemeral storage limit set and constrains the limit to be within the specified maximum values.
            https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          action: DENY
    defaultAction: ALLOW
