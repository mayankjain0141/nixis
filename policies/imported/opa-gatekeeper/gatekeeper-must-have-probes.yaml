# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sRequiredProbes
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-must-have-probes
    annotations:
        aegis.io/imported-from: aegis-import-3633179711.yaml
        aegis.io/original-kind: K8sRequiredProbes
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint must-have-probes (kind: K8sRequiredProbes)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*")
          message: 'OPA Gatekeeper constraint must-have-probes (kind: K8sRequiredProbes)'
          action: AUDIT
    defaultAction: ALLOW
