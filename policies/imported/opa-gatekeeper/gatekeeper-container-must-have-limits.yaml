# IMPORTED FROM: open-policy-agent/gatekeeper-library
CONSTRAINT: K8sContainerLimits
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-container-must-have-limits
    annotations:
        aegis.io/imported-from: aegis-import-2619800462.yaml
        aegis.io/original-kind: K8sContainerLimits
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint container-must-have-limits (kind: K8sContainerLimits)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*")
          message: 'OPA Gatekeeper constraint container-must-have-limits (kind: K8sContainerLimits)'
          action: AUDIT
    defaultAction: ALLOW
