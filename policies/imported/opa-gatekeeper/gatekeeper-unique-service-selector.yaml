# IMPORTED FROM: open-policy-agent/gatekeeper-library
CONSTRAINT: K8sUniqueServiceSelector
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-unique-service-selector
    annotations:
        aegis.io/imported-from: aegis-import-3848820331.yaml
        aegis.io/original-kind: K8sUniqueServiceSelector
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint unique-service-selector (kind: K8sUniqueServiceSelector)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*service")
          message: 'OPA Gatekeeper constraint unique-service-selector (kind: K8sUniqueServiceSelector)'
          action: AUDIT
    defaultAction: ALLOW
