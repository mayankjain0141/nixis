# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-noupdateserviceaccount
    annotations:
        aegis.io/imported-from: aegis-import-4004659431.yaml
        aegis.io/original-kind: NoUpdateServiceAccount
        aegis.io/severity: high
        aegis.io/llm-confidence: medium
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Blocks updating the service account on resources that abstract over Pods. This policy is ignored in audit mode.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: 'tool == "Bash" && request.args.command.matches(".*kubectl.*(patch|edit|update).*serviceaccount.*")'
          message: 'The service account on Deployments, DaemonSets, StatefulSets, and other pod-abstracting resources must not be changed after creation. Service account updates require redeployment.'
          action: DENY
    defaultAction: ALLOW
