# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: NoUpdateServiceAccount
# REGO LOGIC: package noupdateserviceaccount  privileged(userInfo, allowedUsers, _) {   # Allow if the user is in allowedUsers.   # Use object.get so omitted parameters can't cause policy bypass by   # evaluating t...
# MANUAL REVIEW: Pattern inferred from Rego containing 'privileged'
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-noupdateserviceaccount
    annotations:
        aegis.io/imported-from: aegis-import-1996904140.yaml
        aegis.io/original-kind: NoUpdateServiceAccount
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Blocks updating the service account on resources that abstract over Pods. This policy is ignored in audit mode.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*privileged.*")
          message: Blocks updating the service account on resources that abstract over Pods. This policy is ignored in audit mode.
          action: DENY
    defaultAction: ALLOW
