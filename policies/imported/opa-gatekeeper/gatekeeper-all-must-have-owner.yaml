# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sRequiredLabels
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-all-must-have-owner
    annotations:
        aegis.io/imported-from: aegis-import-178667930.yaml
        aegis.io/original-kind: K8sRequiredLabels
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint all-must-have-owner (kind: K8sRequiredLabels)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*")
          message: 'OPA Gatekeeper constraint all-must-have-owner (kind: K8sRequiredLabels)'
          action: AUDIT
    defaultAction: ALLOW
