# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sDisallowedRepos
# REGO LOGIC: package k8sdisallowedrepos  violation[{"msg": msg}] {   container := input.review.object.spec.containers[_]   image := container.image   startswith(image, input.parameters.repos[_])   msg := sprintf("...
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sDisallowedRepos' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8sdisallowedrepos
    annotations:
        aegis.io/imported-from: aegis-import-2686000836.yaml
        aegis.io/original-kind: K8sDisallowedRepos
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Disallowed container repositories that begin with a string from the specified list.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: Disallowed container repositories that begin with a string from the specified list.
          action: DENY
    defaultAction: ALLOW
