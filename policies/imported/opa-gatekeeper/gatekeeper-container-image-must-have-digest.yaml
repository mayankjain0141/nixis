# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sImageDigests
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sImageDigests' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-container-image-must-have-digest
    annotations:
        aegis.io/imported-from: aegis-import-1231042826.yaml
        aegis.io/original-kind: K8sImageDigests
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint container-image-must-have-digest (kind: K8sImageDigests)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: 'OPA Gatekeeper constraint container-image-must-have-digest (kind: K8sImageDigests)'
          action: DENY
    defaultAction: ALLOW
