# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sReplicaLimits
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-replica-limits
    annotations:
        aegis.io/imported-from: aegis-import-3263181151.yaml
        aegis.io/original-kind: K8sReplicaLimits
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint replica-limits (kind: K8sReplicaLimits)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*replicas.*")
          message: 'OPA Gatekeeper constraint replica-limits (kind: K8sReplicaLimits)'
          action: AUDIT
    defaultAction: ALLOW
