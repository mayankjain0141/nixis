# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sPSPAllowPrivilegeEscalationContainer
# REGO LOGIC: 
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sPSPAllowPrivilegeEscalationContainer' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8spspallowprivilegeescalationcontainer
    annotations:
        aegis.io/imported-from: aegis-import-2904223250.yaml
        aegis.io/original-kind: K8sPSPAllowPrivilegeEscalationContainer
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Controls restricting escalation to root privileges. Corresponds to the `allowPrivilegeEscalation` field in a PodSecurityPolicy. For more information, see https://kubernetes.io/docs/concepts/policy/pod-security-policy/#privilege-escalation
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: Controls restricting escalation to root privileges. Corresponds to the `allowPrivilegeEscalation` field in a PodSecurityPolicy. For more information, see https://kubernetes.io/docs/concepts/policy/pod-security-policy/#privilege-escalation
          action: DENY
    defaultAction: ALLOW
