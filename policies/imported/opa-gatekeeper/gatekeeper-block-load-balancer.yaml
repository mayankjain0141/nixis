# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sBlockLoadBalancer
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sBlockLoadBalancer' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-block-load-balancer
    annotations:
        aegis.io/imported-from: aegis-import-2741481742.yaml
        aegis.io/original-kind: K8sBlockLoadBalancer
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint block-load-balancer (kind: K8sBlockLoadBalancer)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: 'OPA Gatekeeper constraint block-load-balancer (kind: K8sBlockLoadBalancer)'
          action: DENY
    defaultAction: ALLOW
