# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sDisallowAnonymous
# REGO LOGIC: package k8sdisallowanonymous  violation[{"msg": msg}] {   not is_allowed(input.review.object.roleRef, object.get(input, ["parameters", "allowedRoles"], []))    group := ["system:unauthenticated", "sys...
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sDisallowAnonymous' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8sdisallowanonymous
    annotations:
        aegis.io/imported-from: aegis-import-3082090923.yaml
        aegis.io/original-kind: K8sDisallowAnonymous
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Disallows associating ClusterRole and Role resources to the system:anonymous user and system:unauthenticated group.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: Disallows associating ClusterRole and Role resources to the system:anonymous user and system:unauthenticated group.
          action: DENY
    defaultAction: ALLOW
