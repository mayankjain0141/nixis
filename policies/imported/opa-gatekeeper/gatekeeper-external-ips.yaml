# IMPORTED FROM: open-policy-agent/gatekeeper-library
CONSTRAINT: K8sExternalIPs
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-external-ips
    annotations:
        aegis.io/imported-from: aegis-import-1080607609.yaml
        aegis.io/original-kind: K8sExternalIPs
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint external-ips (kind: K8sExternalIPs)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*externalIPs.*")
          message: 'OPA Gatekeeper constraint external-ips (kind: K8sExternalIPs)'
          action: DENY
    defaultAction: ALLOW
