# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sRequiredLabels
# REGO LOGIC: 
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8srequiredlabels
    annotations:
        aegis.io/imported-from: aegis-import-581437358.yaml
        aegis.io/original-kind: K8sRequiredLabels
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Requires resources to contain specified labels, with values matching provided regular expressions.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*")
          message: Requires resources to contain specified labels, with values matching provided regular expressions.
          action: AUDIT
    defaultAction: ALLOW
