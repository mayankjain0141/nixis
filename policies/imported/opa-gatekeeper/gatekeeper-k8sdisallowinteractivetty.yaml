# IMPORTED FROM: open-policy-agent/gatekeeper-library
CONSTRAINT: K8sDisallowInteractiveTTY
REGO LOGIC: package k8sdisallowinteractivetty  import data.lib.exempt_container.is_exempt  violation[{"msg": msg, "details": {}}] {     c := input_containers[_]     not is_exempt(c)     input_allow_interactive_fi...
MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sDisallowInteractiveTTY' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8sdisallowinteractivetty
    annotations:
        aegis.io/imported-from: aegis-import-3664732860.yaml
        aegis.io/original-kind: K8sDisallowInteractiveTTY
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Requires that objects have the fields `spec.tty` and `spec.stdin` set to false or unset.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: Requires that objects have the fields `spec.tty` and `spec.stdin` set to false or unset.
          action: DENY
    defaultAction: ALLOW
