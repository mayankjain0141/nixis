# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8spspflexvolumes
    annotations:
        aegis.io/imported-from: aegis-import-2441979100.yaml
        aegis.io/original-kind: K8sPSPFlexVolumes
        aegis.io/severity: high
        aegis.io/llm-confidence: medium
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Controls the allowlist of FlexVolume drivers. Corresponds to the `allowedFlexVolumes` field in PodSecurityPolicy. For more information, see https://kubernetes.io/docs/concepts/policy/pod-security-policy/#flexvolume-drivers
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.matches(".*\\.(yaml|yml|json)$") && request.args.content.contains("flexVolume:")'
          message: 'Pods must not use unapproved FlexVolume drivers. Only allowlisted FlexVolume drivers are permitted.'
          action: DENY
    defaultAction: ALLOW
