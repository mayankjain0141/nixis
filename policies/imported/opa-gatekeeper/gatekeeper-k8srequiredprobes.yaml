# IMPORTED FROM: open-policy-agent/gatekeeper-library
CONSTRAINT: K8sRequiredProbes
REGO LOGIC: package k8srequiredprobes  import data.lib.exclude_update.is_update  probe_type_set = probe_types {     probe_types := {type | type := input.parameters.probeTypes[_]} }  violation[{"msg": msg}] {     ...
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8srequiredprobes
    annotations:
        aegis.io/imported-from: aegis-import-3599354673.yaml
        aegis.io/original-kind: K8sRequiredProbes
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Requires Pods to have readiness and/or liveness probes.
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*(create|apply).*")
          message: Requires Pods to have readiness and/or liveness probes.
          action: AUDIT
    defaultAction: ALLOW
