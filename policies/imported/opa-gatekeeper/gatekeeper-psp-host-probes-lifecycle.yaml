# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sPSPHostProbesLifecycle
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sPSPHostProbesLifecycle' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-psp-host-probes-lifecycle
    annotations:
        aegis.io/imported-from: aegis-import-1254745951.yaml
        aegis.io/original-kind: K8sPSPHostProbesLifecycle
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint psp-host-probes-lifecycle (kind: K8sPSPHostProbesLifecycle)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: 'OPA Gatekeeper constraint psp-host-probes-lifecycle (kind: K8sPSPHostProbesLifecycle)'
          action: DENY
    defaultAction: ALLOW
