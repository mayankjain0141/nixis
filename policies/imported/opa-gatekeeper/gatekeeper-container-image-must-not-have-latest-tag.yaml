# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sDisallowedTags
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-container-image-must-not-have-latest-tag
    annotations:
        aegis.io/imported-from: aegis-import-307579899.yaml
        aegis.io/original-kind: K8sDisallowedTags
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint container-image-must-not-have-latest-tag (kind: K8sDisallowedTags)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*image.*:latest")
          message: 'OPA Gatekeeper constraint container-image-must-not-have-latest-tag (kind: K8sDisallowedTags)'
          action: DENY
    defaultAction: ALLOW
