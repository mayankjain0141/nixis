# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sBlockWildcardIngress
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-block-wildcard-ingress
    annotations:
        aegis.io/imported-from: aegis-import-2453367126.yaml
        aegis.io/original-kind: K8sBlockWildcardIngress
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint block-wildcard-ingress (kind: K8sBlockWildcardIngress)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*ingress.*")
          message: 'OPA Gatekeeper constraint block-wildcard-ingress (kind: K8sBlockWildcardIngress)'
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
