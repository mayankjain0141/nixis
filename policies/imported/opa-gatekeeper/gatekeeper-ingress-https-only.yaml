# IMPORTED FROM: open-policy-agent/gatekeeper-library
# CONSTRAINT: K8sHttpsOnly
# MANUAL REVIEW: IMPORT_TODO: Unknown constraint kind 'K8sHttpsOnly' - manual review required. Rego logic needs human translation.
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-ingress-https-only
    annotations:
        aegis.io/imported-from: aegis-import-2761649679.yaml
        aegis.io/original-kind: K8sHttpsOnly
        aegis.io/severity: high
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: 'OPA Gatekeeper constraint ingress-https-only (kind: K8sHttpsOnly)'
    matchConstraints:
        tools:
            - Bash
    validations:
        - expression: tool == "Bash" && request.args.command.matches("kubectl.*")
          message: 'OPA Gatekeeper constraint ingress-https-only (kind: K8sHttpsOnly)'
          action: DENY
    defaultAction: ALLOW
