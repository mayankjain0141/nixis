# IMPORT_REVIEW: translated from Rego — checks manifest content at Write/Edit time
# imported from: gatekeeper-library via aegis policy import
apiVersion: aegis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8spspautomountserviceaccounttokenpod
    annotations:
        aegis.io/imported-from: aegis-import-496643975.yaml
        aegis.io/original-kind: K8sPSPAutomountServiceAccountTokenPod
        aegis.io/severity: high
        aegis.io/llm-confidence: medium
        aegis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Controls the ability of any Pod to enable automountServiceAccountToken.
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.matches(".*\\.(yaml|yml|json)$") && request.args.content.contains("kind: Pod") && !request.args.content.contains("automountServiceAccountToken: false")'
          message: 'IMPORT_REVIEW: gatekeeper-k8spspautomountserviceaccounttokenpod — check manifest content'
          action: AUDIT
    defaultAction: ALLOW
