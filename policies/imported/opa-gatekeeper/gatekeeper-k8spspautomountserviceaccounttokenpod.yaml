# imported from: gatekeeper-library via nixis policy import
apiVersion: nixis.io/v1
kind: PolicyTemplate
metadata:
    name: gatekeeper-k8spspautomountserviceaccounttokenpod
    annotations:
        nixis.io/imported-from: nixis-import-496643975.yaml
        nixis.io/original-kind: K8sPSPAutomountServiceAccountTokenPod
        nixis.io/severity: high
        nixis.io/llm-confidence: medium
        nixis.io/source: open-policy-agent/gatekeeper-library
spec:
    description: Controls the ability of any Pod to enable automountServiceAccountToken.
    matchConstraints:
        tools:
            - Write
            - Edit
    validations:
        - expression: '(tool == "Write" || tool == "Edit") && request.args.path.matches(".*\\.(yaml|yml|json)$") && request.args.content.contains("kind: Pod") && !request.args.content.contains("automountServiceAccountToken: false")'
          message: 'Pods must not enable automountServiceAccountToken unless explicitly required.'
          action: REQUIRE_APPROVAL
    defaultAction: ALLOW
