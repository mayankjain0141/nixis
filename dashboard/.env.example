# Nixis Dashboard environment variables
# Copy to .env and adjust as needed: cp .env.example .env

# Daemon URL — required for production builds (npm run build)
# Not needed for dev server (defaults to http://127.0.0.1:9090)
VITE_DAEMON_URL=http://127.0.0.1:9090
